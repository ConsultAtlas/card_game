// lets create a variable that represents a playing card

//var car = {
// suit: "Hearts".
// number: 13,
// color: "red",
// isFaceDown: true
//}

// lets build a deck of cards

var deck = [];

for (var i=0; i<52; i++) {
	var number = i % 13;
	var car = {
		suit: "Hearts".
		number: number+1,
		color: "red",
		isFaceDown: true
    }
    deck.push(card);
}